import 'package:flutter/material.dart';

class AppColors {
  // Use 'static const' for better performance
  static const Color background = Color(0xFF0A1208); 
  static const Color surface = Color(0xFF131D12);
  static const Color primary = Color(0xFF1ED700); 
  static const Color danger = Color(0xFFFF3B30); // Vibrant "System" Red
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
}